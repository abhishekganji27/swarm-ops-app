services:
  backend:
    build: ./backend
    # docker hub image
    # image: abhishekganji27/docker-swarm-dashboard
    
    # registry service image
    # image: 127.0.0.1:5009/swarmops-test
    ports:
      - "5000:5000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

    deploy:
      placement:
        constraints:
          - "node.role==manager"
  
  frontend:
    
    build: ./frontend
    
    ports:
      - "5173:80"

    deploy:
      placement:
        constraints:
          - "node.role==manager"